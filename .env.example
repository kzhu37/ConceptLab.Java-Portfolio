# Desktop: set these values in your local process environment only.
# Browser deployment: configure the same names as private Vercel environment variables.
# Never commit real API keys and never expose them through public-prefixed variables.

GROQ_API_KEY_PRIMARY=your_primary_groq_api_key_here
GROQ_API_KEY_SECONDARY=your_optional_secondary_groq_api_key_here
